<h3><%= @post.title %></h3>
<h4>Written on: <%= @post.date %></h4>

<p><%= @post.body %></p>
<hr>
<h3>Comments</h3>
  <% if @comments.any? %>
    <ul>
      <% @comments.each do |comment| %>
        <li>Posted by: <%= comment.name %> | At: <%= comment.when %>
          <%= comment.content %> </li>
      <% end %>
    </ul>
  <% else %>
    <p>No comments on this post.</p>
  <% end %>

<hr>
<h4>Add a comment to this post:</h4>

<%= render 'comments/form' %>

<hr>
<%= link_to 'Return to posts list', posts_path %> |

<%= link_to 'Edit this post', edit_post_path(@post) %> |

<%= link_to 'Delete this post', post_path(@post),
:data => {:confirm => 'Are you sure? There is no way to undo this', :method => 'delete' } %>
